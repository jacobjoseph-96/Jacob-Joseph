{
  "name": "jacob-joseph-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./input.css -o ./style.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
